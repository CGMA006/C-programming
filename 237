#include <stdio.h>

union Data {
    int i;
    float f;
};

void printUnion(union Data data) {
    printf("%d\n", data.i);
}

int main() {
    union Data data;
    data.i = 100;
    printUnion(data);
    return 0;
}
