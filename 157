#include <stdio.h>
int main() {
    for (int i = 1; i <= 5; i++) {
        switch (i) {
            case 2:
                continue;
            case 4:
                continue;
        }
        printf("%d ", i);
    }
    
    return 0;
}
