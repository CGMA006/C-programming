#include <stdio.h>
int main() {
    int a = 5;
    double b = 2.0;
    double result = a + b;  // Implicit conversion of int to double in the expression
    printf("Result: %.2lf", result);
    return 0;
}
