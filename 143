#include <stdio.h>
int main() {
    for (int i = 1; i <= 5; i++) {
        if (i == 3) {
            goto exit_loop;
        }
        printf("%d ", i);
    }
    
exit_loop:
    printf("\nExited loop early");
    return 0;
}
